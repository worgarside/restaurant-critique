extends partials/layout

block content
    br
    .container
        .row
            .col
                h1 Get in touch
                p We would love to hear your thoughts on our services
        .row
            .col
                .form-row
                    .form-group.col
                        form(action='/contact' method='POST' id='contact-form')
                            .form-row
                                .form-group.col
                                    label(for='contact-name') Name
                                    input#contact-name(type='text', name='name', placeholder='Your name' required)
                            .form-row
                                .form-group.col
                                    label(for='contact-email') Email
                                    input#contact-email(type='text', name='email', placeholder='Your email' required)
                            .form-row
                                .form-group.col
                                    label(for='contact-message') Message
                                    textarea#contact-message(name='message', placeholder='Your message', required)
                            .form-row
                                .form-group.col
                                    input.btn(type='submit', value='Submit')
